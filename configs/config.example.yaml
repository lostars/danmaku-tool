#  弹幕保存路径 只作用于CLI模式
save-path: ""
# 合并毫秒内重复弹幕 单位 ms <=0则不启用
merge-danmaku-in-mills: 1000
bilibili:
#  SESSDATA 开头的cookie部分即可
  cookie: ""
#  单个视频弹幕下载的并发数 调高了注意账号风险 用的b站6min一段的弹幕接口，调高了意义也不大
  max-worker: 4
#  请求超时时间 单位：秒
  timeout: 10